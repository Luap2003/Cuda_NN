// main.cu
#include "neural_net.h"
#include "utils.h"
#include <stdio.h>

int main() {
    // Load data
    float *x_train, *y_train;
    int num_samples, input_size, output_size;
    
    int epochs = 10;
    int batch_size = 32;

    
    free(x_train);
    free(y_train);

    return 0;
}
